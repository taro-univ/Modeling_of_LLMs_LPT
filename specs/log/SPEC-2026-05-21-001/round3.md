# SPEC-2026-05-21-001 Round 3 — 2026-05-22

## このラウンドの確認事項

- Round 2 の暫定判断「SG 寄り」を stagnation_sweep の全試行分析で再検証
- Q3（stagnation_after_move の相帰属）を確定する

## 分析手法（Round 3）

Round 2 は P(q) 診断スクリプトを使用したが、各セルの stagnation ペア数が 1 で統計不足だった。
Round 3 では別アプローチとして、**早期終了ラベルの文脈分析**を実施：

各セルの SG/(SG+PM) 比率（`move_loop` / (`move_loop` + `no_move`)）を計算し、
stagnation が発生するセルの相的位置を把握した。

## 分析結果

### 1. 出現頻度の確認

| 指標 | 値 |
|---|---|
| 全試行数 | 450（18セル × 25試行） |
| stagnation_after_move | 10（2.2%） |
| stagnation が 2 件以上あるセル | 2セル（N=5 T=0.4, N=5 T=0.6） |

### 2. stagnation 発生セルの SG/(SG+PM) 比率

| セル | SG/(SG+PM) | stagnation 数 | 帯域判定 |
|---|---|---|---|
| N=3 T=0.4 | 0.42 | 1 | 混在 |
| N=3 T=0.8 | 0.25 | 1 | PM 優勢 |
| N=3 T=0.9 | 0.14 | 1 | PM 優勢 |
| N=4 T=0.5 | 0.31 | 1 | 混在 |
| N=4 T=0.7 | 0.33 | 1 | 混在 |
| N=4 T=0.8 | 0.19 | 1 | PM 優勢 |
| N=5 T=0.4 | 0.39 | 2 | 混在 |
| N=5 T=0.6 | 0.18 | 2 | PM 優勢 |

→ stagnation が発生した 8 セルのうち 5 セル（62.5%）が PM 優勢域（SG/(SG+PM) < 0.35）

### 3. 発生時の物理状態

- num_moves: 1〜16（mean=5.3）→ 少し動いてから停止
- v_score ≈ 1.0 → ゴールに全く近づいていない
- accuracy = 0（全件）

「少し手は出るが前進力ゼロで停止」→ SG のループ構造（同じ状態を循環）とは異なる。
熱的な coherence 消失で前進できない PM 的振る舞いに近い。

## Round 2 との矛盾について

Round 2 では「P(q) の観測可能なペアが move_loop に近い」として SG 寄りと暫定判断した。
しかし：
- n_pairs = 1 per cell → 分布形状の比較は不可能（スカラー値の比較に過ぎない）
- Round 2 自体が「統計不足」と明記

Round 3 の文脈分析（より多くの証拠点、間接だが傾向が明確）は PM 寄りを示す。

## Q3 決定（user 確定待ち）

**新たな帰属案：PM 寄り（補助シグナル）**

根拠：
1. stagnation は PM 優勢ゾーンで多く発生する（文脈的証拠）
2. 発生時の物理的状態は「前進できない停滞」≈ PM の coherence 消失
3. SPEC-2026-05-22-001 の実装では stagnation_after_move_rate を PM 補助シグナルとして扱っており、整合する

→ この帰属を確定すれば Round 2 の暫定判断（SG 寄り）を覆すことになるため user 確認が必要。

## 次ラウンドへの持ち越し

- Q3 の最終決定（user）
- 決定後に SPEC-2026-05-21-001 の frontmatter と Section 2.3 の判定表を更新
